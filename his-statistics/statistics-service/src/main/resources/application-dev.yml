#spring的配置
spring:
  #数据源的配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rm-bp1637o574j7oqkhgbo.mysql.rds.aliyuncs.com:3306/hospital?useUnicode=true&characterEncodeing=UTF-8&useSSL=false&serverTimezone=GMT
    username: admin_user
    password: Huang5583
    #注入数据源的类型   默认的为HikariDataSource
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      max-active: 20
      min-idle: 5
      validation-query: select x
      initial-size: 3
      max-wait: 5000
      stat-view-servlet:
        #        login-username: root
        #        login-password: 123456
        allow:
        deny:
        url-pattern: /druid/*
        enabled: true  #启用数据源监控
#mybatisplus的配置
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*Mapper.xml
  global-config:
    db-config:
      id-type: auto
#dubbo配置
dubbo:
  application:
    name: his-statistics-service
  registry:
    address: zookeeper://47.97.82.55:2181
    timeout: 100000
  protocol:
    name: dubbo
    port: -1
  consumer:
    check: false
  config-center:
    timeout: 25000
